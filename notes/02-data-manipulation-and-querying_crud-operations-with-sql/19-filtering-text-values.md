# Filtering text values

```sql
SELECT *
FROM sales
WHERE customer_name != 'Max Schwarz';
-- ------
SELECT *
FROM sales
WHERE customer_name > 'Company A';
```

Next: [Working with dates](./20-working-with-dates.md)
