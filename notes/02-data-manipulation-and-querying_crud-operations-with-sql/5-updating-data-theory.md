# Updating data theory

- Update all identified rows in a specified table and set the specified columns to new values
- Multiple rows can be updated in one operations (if the conditions is mer by multiple rows)

```sql
UPDATE <table name>
SET <column name> = <new Value>, -- can update multiple
WHERE <condition>;
```

Next: [Deleting data theory](./6-deleting-data-theory.md)
